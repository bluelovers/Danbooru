<div>
  <h5>Tags</h5>
  <ul id="tag-sidebar">
    <%= tag_links(@tags[:exclude], :prefix => "-") %>
    <%= tag_links(@tags[:include]) %>
    <%= tag_links(Tag.find_related(@tags[:related].to_a)) %>
  </ul>
</div>
