<div id="forum">
<table>
	<tr>
		<th>Title</th>
		<th>Author</th>
		<th>Last Updated</th>
	</tr>
<% @forum_posts.each do |fp| %>
	<tr class="<%= cycle 'even', 'odd' %>">
		<% if @current_user && fp.updated?(@curent_user) %>
			<td><strong><%= link_to fp.title, :action => "show", :id => fp.id %></strong></td>
		<% else %>
			<td><%= link_to fp.title, :action => "show", :id => fp.id %></td>
		<% end %>
		<td><%= fp.author %></td>
		<td><%= fp.updated_at.strftime("%D %I:%M %p") %></td>
	</tr>
<% end %>
	<tr class="footer">
		<td colspan="3"><%= link_to "New Topic", :action => "add" %></td>
	</tr>
</table>
</div>
