<h4>Invites</h4>
<p>You can vouch for existing members and invite them to contributor status.</p>

<div style="margin-bottom: 2em">
  <h6>Invite User</h6>
  <% form_tag({:action => "invites"}, :onsubmit => "return confirm('Are you sure you want to invite ' + $F('user_name') + '?')") do %>
    <table width="100%">
      <tfoot>
        <tr>
          <td colspan="2"><%= submit_tag "Submit" %></td>
        </tr>
      </tfoot>
      <tbody>
        <tr>
          <td><label for="member_name">Name</label></td>
          <td><%= text_field_with_auto_complete "member", "name", {:value => params[:name]}, :min_chars => 3, :skip_style => true, :url => {:controller => "user", :action => "auto_complete_for_member_name"} %></td>
        </tr>
        <tr>
          <td><label for="member_level">Level</label></td>
          <td><%= select "member", "level", [["Contributor", CONFIG["user_levels"]["Contributor"]], ["Builder", CONFIG["user_levels"]["Builder"]], ["Privileged", CONFIG["user_levels"]["Privileged"]]] %></td>
        </tr>
      </tbody>
    </table>
  <% end %>
</div>

<%= render :partial => "footer" %>